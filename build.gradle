group 'cf.nathanpb'
version '3.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
jar {
    from zipTree('libs/ProjectMetadata-1.0-SNAPSHOT-all.jar')
    from zipTree('libs/javassist.jar')
    from zipTree('libs/reflections-0.9.10.jar')
}
